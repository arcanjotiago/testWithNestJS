import type {Config} from 'jest';

const config: Config = {
    verbose: true,
    collectCoverage: true //ref here: https://jestjs.io/docs/configuration#collectcoverage-boolean
};

export default config;